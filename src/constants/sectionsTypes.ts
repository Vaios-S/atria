import type { SpaceSectionType } from "../types/spaceSection";

export const SPACE_SECTION_LABELS: Record<SpaceSectionType, string> = {
  quests: "Quests",
  checklist: "Checklist",
  notes: "Notes",
};
